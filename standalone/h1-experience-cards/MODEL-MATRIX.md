# MODEL-MATRIX — H1 experience cards

Generated by `bin/compare.py` from the run dirs — no hand-typed
numbers. One row per measured (stage S2) extract model.

| model | unlock_hit_label | wrong | abstain | serve_rate | cluster_purity | reject_rate | judge faithful | USD/1000 | p50 ms | verdict |
|--|--|--|--|--|--|--|--|--|--|--|
| _(no measured S2 run yet)_ | | | | | | | | | | |

## Add an arm in three steps

```
1. python bin/run_experiment.py --stage S2 --model <new-model> --out runs/<date>_S2_<model>
2. python bin/compare.py runs/*/ > MODEL-MATRIX.md
3. read the new row next to B1 — if the new arm is at or below B1, the cards
   add nothing on that model, and that is the finding
```

