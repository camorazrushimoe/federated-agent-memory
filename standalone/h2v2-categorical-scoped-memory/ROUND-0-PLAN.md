# H2v2 — план первого раунда

Не продолжение Phase C v1. Новая комиссия.

## Что уже закрыто (не делать)

- D0 gold_useful на срезе 60 — канон v1, lead sign-off #51 / #60.
  Файл скопирован: `data/gold_useful.jsonl`. **NOT human gold.**
- Контур S1–S7 + eval + replay + checks — скопирован из v1 `bin/`.
- Вердикт v1 NOT FIT — [`WHY-NOT-V1.md`](./WHY-NOT-V1.md).

## Что сдать в этом раунде

| # | что | гейт |
|---|---|---|
| V2-D1 | словарь заморожен в CATEGORIES.md + config.py + PROMPTS §2 | список id совпадает во всех трёх местах |
| V2-D2 | S0 на фикстурах новым тегером | HARD блока S0 зелёные; gold_tags — категории |
| V2-D3 | S2 на pool 320 + slice 60, новый run id | распределение категорий в audit/report |
| V2-D4 | гейт бакетов (README) | медиана кандидатов = десятки; не 0 и не 320 |
| V2-D5 | если D4 зелёный: T/B0–B3 на том же gold_useful | одна строка FIT / FIT WITH LIMITS / NOT FIT |

D4 красный → стоп, корпус 1000+200 не открывать, промпт не крутить «ради hit».

## Запреты

- `unlock` / `unlock_guideline` на вход S2
- импорт H1-кода
- эмбеддинги и Jaccard как основная ось
- правка порогов после увиденного T.hit
- переразметка gold_useful без отдельного решения основателя
