# Hypothesis 2 — сессии как подсказки

Это упрощённая постановка эксперимента.
Что выкинули из старого POC — в [`SIMPLIFICATIONS.md`](./SIMPLIFICATIONS.md).
Контракт шагов — [`SPEC.md`](./SPEC.md).
Все строки модели и пакет агента — [`PROMPTS.md`](./PROMPTS.md).
Как меряем — [`EVAL-PLAN.md`](./EVAL-PLAN.md).
Проводку шагов — [`CHECKS.md`](./CHECKS.md).
Как гонять прогон — [`RUN-PROTOCOL.md`](./RUN-PROTOCOL.md).
Бриф для лабы — [`LAB-BRIEF.md`](./LAB-BRIEF.md).
S0-фикстуры — [`fixtures/`](./fixtures/).

## Идея в одном абзаце

После каждой сессии мы не делаем короткую карточку, как в H1.
Мы размечаем **всю сессию** метаданными и кладём её в общий пул.
Когда начинается новый диалог, система по этим метаданным находит похожие старые сессии и подмешивает их агенту как подсказку.
Потом смотрим, сессия прошла лучше или хуже — и поднимаем или опускаем рейтинг тех сессий, которые подмешали.

Все агенты одинаковые. Один тенант. Общий пул сессий.

## Зачем это, если уже есть H1

H1 сжимает сессию в карточку и отдаёт карточку.
H2 проверяет другой ход: **отдать целую похожую сессию** и понять, помогает это или мешает.

Карточка дешевле и чище.
Целая сессия может содержать ход мысли, который в карточку не влез.
Эксперимент как раз про то, стоит ли этот объём своих денег.

## Два куска системы

### 1. Компилятор — разметка

На вход: закончившийся диалог.
На выход: та же сессия плюс метаданные.

Метаданные нужны только чтобы потом быстро найти похожую сессию.
Не чтобы решать «private или global». Этого шага больше нет.

Качество разметки меряем отдельно: совпали ли теги с человеческой разметкой.

Теги заморожены в SPEC §4 и промпте S2:
`problem_shape`, `constraint`, `ending`, плюс `channel` и `vertical` с диалога.

Единственный обязательный вызов LLM в v1 — эта разметка. Текст: [`PROMPTS.md` §2–§3](./PROMPTS.md).

### 2. Ранкер — польза и ротация

На вход: текущий диалог (его черновые теги) + кандидаты из пула.
На выход: какие старые сессии подмешать и в каком порядке.

Подмешиваются не «кусочки знаний», а **целые прошлые сессии** с похожими метаданными.

После того как новая сессия закончилась, нужен измеритель исхода:

- сессия прошла хорошо
- сессия прошла плохо
- непонятно

По этому сигналу ранкер обновляет рейтинг подмешанных сессий
**в связке с тем набором тегов, по которому их нашли**.

Одна и та же старая сессия может быть полезна для одних тегов и вредна для других.

Рейтинга мало. Если всегда отдавать текущий топ, картинка застынет:

- вчерашние лучшие сессии займут все слоты;
- их реальная польза может уже упасть;
- новые и редко показанные сессии не получат шанса.

Поэтому ранкер мешает две вещи: **пользу** и **ротацию**.

Простое правило на сейчас:

1. Обычно берём сессии с высоким рейтингом пользы.
2. Один слот оставляем «на пробу»: сессия, которую давно не показывали, или у которой мало показов.
3. Если одну и ту же сессию крутим слишком часто, её рейтинг чуть оседает сам — даже без плохого исхода. Иначе топ никогда не разъедется.

Так бывший лидер может постепенно опуститься, а забытая сессия — всплыть и получить новую оценку.

Поиск и ранжирование без LLM. Пакет собирается шаблоном [`PROMPTS.md` §5](./PROMPTS.md).

## Как это должно работать по шагам

```
сессия закончилась
        ↓
компилятор повесил метаданные          S2  LLM
        ↓
сессия попала в общий пул
        ↓
начался новый диалог
        ↓
по тегам текущего диалога ищем похожие сессии   S3  без LLM
        ↓
ранкер берёт полезные + иногда одну забытую     S4  без LLM
        ↓
эти сессии подмешиваются агенту как подсказка   S5  шаблон, без LLM
        ↓
новая сессия закончивается
        ↓
измеритель говорит: помогло / навредило / неясно   S6  gold, не модель
        ↓
рейтинг подмешанных сессий обновляется          S7  без LLM
        ↓
у слишком часто показываемых рейтинг чуть оседает
```

## Что подмешивается в новую сессию

Не карточки и не отдельные реплики.
Целые прошлые сессии, которые компилятор пометил похожими тегами.

Агент смотрит чужой закрытый диалог и пытается вытащить оттуда ход, который сработал.

В пакет не больше `MAX_PACKET` сессий (сейчас 3, один слот — ротация).
В промпт нельзя сваливать весь пул. Ранкер выбирает, что из найденного стоит показывать.

Целая сессия в промпте дорогая. Нарезку на мелкие куски не делаем в этой версии, но это не мусор, а следующий рычаг экономии токенов. См. лог.

## Что считаем в эксперименте

Четыре разные цифры, не смешивать. Подробности и пороги — [`EVAL-PLAN.md`](./EVAL-PLAN.md).

1. **Разметка.** `ending` / `constraint` / `problem_shape` считаются порознь против человеческих тегов.
2. **Польза.** В пакете оказалась золотая полезная сессия чаще, чем без пакета и чаще, чем при случайной похожей. Отдельно считаем `wrong`: пакет непустой, а полезной сессии в нём нет.
3. **Ротация.** На длинном прогоне выдача не застыла в одних и тех же 1–3 id; explore-слот живой.
4. **Цена.** Токены разметки + токены целого пакета. Даже если польза есть, дорогой пакет — отдельный предел, а не «значит, идея не работает».

Если разметка плохая, ранкер не виноват: он ищет не туда.
Если разметка хорошая, а исходы не лучше случайной похожей — не работает идея «подмешивать целую сессию через ранкер».

## Чего в этой версии нет

- тенантов и групп агентов
- private / shared / global
- решения видимости по слову `policy` или `incident`
- своей памяти агента между сессиями
- склеивания нескольких сессий в одну каноническую историю
- нарезки сессии на мелкие куски (отложили из-за простоты; вернём ради токенов, если идея живая)
- LLM на поиске, ранжировании и (в лабораторном прогоне) на исходе

Это сознательно. См. лог упрощений.
