# H2 — бриф для лабы

Документ передачи. Отдать инженеру вместе с репозиторием.
Рабочая доска с галками — [`CHECKLIST.md`](./CHECKLIST.md). Этот файл — заказ, роли и стопы.

Читать в таком порядке: [`README.md`](./README.md) → [`SPEC.md`](./SPEC.md) → [`PROMPTS.md`](./PROMPTS.md) → [`EVAL-PLAN.md`](./EVAL-PLAN.md) → [`CHECKS.md`](./CHECKS.md) → [`RUN-PROTOCOL.md`](./RUN-PROTOCOL.md) → [`CHECKLIST.md`](./CHECKLIST.md).

**Заказ:** собрать runnable-контур «целая сессия как подсказка», измерить его и ответить одной строкой: **FIT / FIT WITH LIMITS / NOT FIT**.

**Чего нет в заказе:** новая идея, свой датасет «потому что этот некрасивый», эмбеддинги, сервис, база, импорт H1-кода, сравнение hit с карточками H1, нарезка сессии, вторая модель «на всякий случай».

---

## 0. Первые полчаса

1. Открыть этот каталог. Код и состояние живут только здесь.
2. Прогнать глазами фикстуры: `d-006` должен отвалиться на ingest, `d-005` содержит PII, `d-007` — query, золото полезных = `d-001` и `d-002`, `d-014` другой tenant.
3. Поставить галки раздела 0 в CHECKLIST.
4. Писать S1. Не начинать корпус, пока S0 на фикстурах не зелёный.

Не читать код H1. Схему диалога можно скопировать глазами из SPEC §3.

---

## 1. Роли

| роль | владеет |
|---|---|
| **Research Engineer** | скрипты S1–S7, `replay.py`, `eval.py`, харнесс чеков, run dir |
| **Research Lead** | ревью **перезапуском**, не чтением; аудит A1–A6; вердикт fitness |
| **Evaluation** | только L3, если его откроют; в первом проходе можно не открывать |

Кто написал модуль, тот его не утверждает.
Один модуль — один владелец. Второй параллельный `bin/` заводить нельзя: в H1 это уже стоило раунда.

Lead не принимает D1 по диффу. Принимает, если заново прогнал блок S0 из CHECKS и увидел те же JSON.

---

## 2. Что сдать, по порядку

Галка и выход из гейта — в [`CHECKLIST.md`](./CHECKLIST.md). Здесь только смысл пакета.

| # | артефакт | гейт дальше |
|---|---|---|
| **D1** | `bin/` по SPEC: `ingest`, `tag`, `retrieve`, `rank`, `mix`, `outcome`, `update`, `replay` | каждый шаг на [`fixtures/`](./fixtures/) даёт ожидаемый JSON |
| **D2** | харнесс: все id из CHECKS, HARD валит прогон | S0: HARD из блока «Что прогонять на S0» зелёные |
| **D3** | `audit.json` — A1–A6 из EVAL-PLAN §7 | пороги §6.2 достижимы или поправлены **в EVAL-PLAN до** полного прогона |
| **D4** | руки B0/B1/B2/B3 и T через один подсчёт классов | `C-EV1`–`C-EV4` зелёные |
| **D5** | один измеряемый прогон (стадия S2), один run dir | `manifest.json`, `checks.json`, `metrics.json`, `cost.json`, `per_query.jsonl`, `report.md` |
| **D6** | L3 | только если золота не хватило; иначе строка «L3 закрыт, золото есть» |
| **D7** | вердикт ровно одной строкой EVAL-PLAN §6.4 | без второй руки модели |

D5 — продукт. D1–D4 нужны, чтобы D5 не врал.
Пакет для чужого клона (как H1 D8) в этом проходе не обязателен. Но `--replay` без LLM MUST работать у того, кто пишет код, и raw S2 MUST сохраняться: иначе следующий человек не пересчитает цифру.

Нельзя сдать D5, пока D2 красный. Нельзя публиковать L2 по пользе, пока нет `gold_useful` на корпусе.

---

## 3. Модель и ключи

Единственный живой вызов в измеряемом контуре — S2 tag.

| слот | модель | статус |
|---|---|---|
| `tag` | `deepseek-v4-flash` | рука первого прохода; смена — флаг, не правка |
| `judge` | нет | L3 закрыт, пока не скажут иначе |

```
export H2_API_KEY=<key>
export H2_BASE_URL=https://api.deepseek.com/v1
```

В `bin/` нет литерала модели, урла и ключа. Чужой ключ H1 (`H1_API_KEY`) не читать.

Температура 0. Свой system/user к PROMPTS.md не дописывать.

---

## 4. Данные

- S0: только [`fixtures/`](./fixtures/). Этого хватает, чтобы закрыть D1–D2.
- Дальше: схема диалога как в H1. Корпус можно **скопировать** из `standalone/h1-experience-cards/data/`, код H1 копировать нельзя.
- `unlock` / `unlock_guideline` / `split` из пакета H1 — не золото H2. На ingest их выкинуть.
- `gold_tags` пишут люди. `gold_useful` на срезе корпуса — агентная разметка
  `deepseek-v4-pro` по решению основателя 2026-08-28, **NOT human gold**
  ([`ROUND-0-PLAN.md`](./ROUND-0-PLAN.md)). Пока этих файлов нет на корпусе —
  L2 по пользе не публиковать. L0/L1 и разметка на фикстурах живут отдельно.
- `tenant_id` в сырой строке MAY остаться. На поиск и рейтинг не влияет (`C-ISO4`).

Маппинг полей H1 → H2 — RUN-PROTOCOL §2.2. Не держать его в голове: он заморожен там.

---

## 5. Как гонять

Одна команда, одна папка, один манифест — [`RUN-PROTOCOL.md`](./RUN-PROTOCOL.md).

Стадии EVAL-PLAN §9: S0 smoke → S1 dev → S2 full → (S3) → S4 verdict.
Hold-out / золотые useful-пары замораживать до S2. Дописывать пары после увиденного `hit` нельзя.

Пока нет runner, инженер гоняет шаги по одному:

```
python bin/ingest.py   --in fixtures/dialogues.jsonl
python bin/tag.py      --in data/dialogues.jsonl
python bin/retrieve.py --query fixtures/queries/d-007.json --pool data/sessions.jsonl
python bin/rank.py     --candidates data/candidates.jsonl --ratings data/ratings.jsonl --tag-key ...
python bin/mix.py      --ranked data/ranked.jsonl --pool data/sessions.jsonl
python bin/outcome.py  --query ... --packet data/packet.json --source gold --gold fixtures/gold_useful.jsonl
python bin/update.py   --outcome data/outcomes.jsonl --ratings data/ratings.jsonl
```

Когда runner есть — только он. Свою логику в `replay.py` не класть.

Порядок на одном диалоге внутри replay (SPEC §8), иначе сломается `C-FUTURE`:

1. S3–S5 по уже лежащему пулу
2. S6 исход
3. S7 рейтинг пакета
4. S2 разметить текущий диалог и положить в пул

---

## 6. Что нельзя перепутать

Четыре числа, четыре разных отказа:

1. Плохие теги → чинить S2 / промпт (промпт заморожен: чинить только после нового run id).
2. Теги живые, поиск пустой на золотой паре → S3 или `TAG_FIELDS_MIN`, не ранкер.
3. Поиск живой, в пакете одни и те же id → ротация, смотреть `top3_share` и B2.
4. `hit` выше B1, пакет огромный → это не провал идеи, это цена. Вердикт `FIT WITH LIMITS`, рычаг — нарезка. Нарезку в этом проходе не делать.

L1 не тащить в отчёт как «система работает».
T без B1 не тащить как результат.

Карта симптом → шаг лежит в конце CHECKLIST. Не изобретать пятую.

---

## 7. Стоп и эскалация

Остановиться и написать заказчику, если:

- аудит A1–A6 показывает, что порог §6.2 недостижим;
- HARD не проходит без правки SPEC;
- провайдер не отвечает так, что прогон нельзя повторить;
- хочется завести второй `bin/` или поменять промпт «чтобы цифра выросла»;
- хочется открыть L3 или вторую модель без отдельного решения.

Порог после увиденного числа не двигаем. Только до прогона, только в EVAL-PLAN, только с арифметикой.

---

## 8. Формат отчёта за раунд

Без воды. Одна карточка на сдачу D1…D7.

```
ROUND n · FROM: <роль>
DELIVERABLE: D<k> — <одна строка>
ARTIFACT: <sha / run_id>
NUMBERS: <только измеренные, с n>
CHECKS: <hard passed>/<hard total>, soft: <n>
BLOCKED: <что, или nothing>
NEXT: <одно следующее действие>
```

Вердикт в конце D7 — ровно одна строка §6.4, не абзац.

Lead отвечает на карточку либо «перезапустил, принял», либо конкретным красным id из CHECKS / EVAL-PLAN. «Выглядит нормально» — не приёмка.
