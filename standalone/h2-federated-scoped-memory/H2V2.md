# H2 v1 is closed — NOT FIT

Do not patch this folder to chase a T > B1 gap. The next experiment lives in:

[`../h2v2-categorical-scoped-memory/`](../h2v2-categorical-scoped-memory/)

Evidence: PR #63 (R3), PR #64 (R4). Handoff comment on #64.
