# H2 — рабочий чек-лист лабы

Это доска, не спецификация.
Контракт шага — [`SPEC.md`](./SPEC.md).
Id проверок — [`CHECKS.md`](./CHECKS.md).
Заказ и стоп-условия — [`LAB-BRIEF.md`](./LAB-BRIEF.md).
Как гонять — [`RUN-PROTOCOL.md`](./RUN-PROTOCOL.md).

Ставить `[x]` только после перезапуска, не после «я посмотрел глазами».
Не прыгать через гейт. D5 без зелёного D2 — это не измерение.

---

## 0. До первой строки кода

- [ ] Прочитаны README → SPEC → PROMPTS → EVAL-PLAN → CHECKS → RUN-PROTOCOL → этот файл
- [ ] Понятно, что живой LLM один: S2 tag. Остальные шаги без модели
- [ ] Понятно, что лабораторный исход S6 берётся из золота, не из судьи
- [ ] Каталог только `standalone/h2-federated-scoped-memory/`
- [ ] Ключ: `H2_API_KEY` + `H2_BASE_URL`. `H1_API_KEY` не читать
- [ ] Второй `bin/` не заводить

Готово, когда можно вслух сказать четыре разные цифры: теги, hit/wrong, ротация, токены.

---

## D1 — скрипты по SPEC

Гейт: каждый шаг на [`fixtures/`](./fixtures/) даёт ожидаемый JSON.
Команды — LAB-BRIEF §5.

### Общее на `bin/`

- [ ] Есть `ingest.py` `tag.py` `retrieve.py` `rank.py` `mix.py` `outcome.py` `update.py` `replay.py`
- [ ] Есть одна обёртка `call_llm(system, user)`
- [ ] В `bin/` нет литерала модели, урла, ключа
- [ ] Нет импорта из `research/`, `openspec/`, H1, GitLab-POC (`C-ISO1`)
- [ ] Нет эмбеддингов и драйверов БД (`C-ISO2`)

### S1 `ingest.py`

- [ ] `kept + dropped` сходится со входом (`C-IN1`)
- [ ] `d-006` отброшен, у kept есть customer-turn (`C-IN2`)
- [ ] Поля схемы SPEC §3 на месте (`C-IN3`, `C-IN4`)
- [ ] Повторный ingest — байт-тот же файл, не append (`C-IN5`)
- [ ] LLM не зовётся, теги не ставятся (`C-IN6`)

### S2 `tag.py`

- [ ] system/user ровно PROMPTS §2–§3, транскрипт по §1 (`C-PROMPT`)
- [ ] `session_id = s-` + sha256(dialogue_id)[:12] (`C-TG1`)
- [ ] `channel` / `vertical` с диалога, не из модели (`C-TG2`)
- [ ] `tag_key` склеен без краевых пробелов (`C-TG3`)
- [ ] `ending` из четырёх значений, поля длины держатся (`C-TG4`)
- [ ] Повторный tag той же id не плодит вторую сессию (`C-TG5`)
- [ ] `d-005` жив, PII вычищен, `contains_pii=true` (`C-TG6`, `C-TG7`, `C-PII`)
- [ ] «gift card» сам не ставит PII (`C-TG8`)
- [ ] Два битых JSON подряд → reject, теги не выдуманы (`C-TG9`)
- [ ] На каждый вызов есть `raw/tag/<id>.json` (`C-TG10`)
- [ ] Новая сессия получает rating `score=0`, `shows=0` (`C-TG11`)

### S3 `retrieve.py`

- [ ] Query не в кандидатах (`C-SELF`)
- [ ] Кандидат совпал хотя бы по `TAG_FIELDS_MIN` полям (`C-RT1`)
- [ ] `d-007` видит `d-001` и `d-002` (`C-RT2`)
- [ ] `d-003` не обязан быть у `d-007` (`C-RT3`)
- [ ] `d-014` с другим tenant находится так же (`C-ISO4`)
- [ ] Шаг сам модель не зовёт (`C-RT4`)
- [ ] Повтор на том же пуле — тот же набор id (`C-RT5`)

### S4 `rank.py`

- [ ] Топ-слоты по `score` пары `(session_id, tag_key query)` (`C-RK1`)
- [ ] Последний слот — explore: меньше `shows`, затем старше `last_shown_at`, затем меньший id (`C-RK2`)
- [ ] Explore не дублирует уже выбранный id (`C-RK3`)
- [ ] На корзине login к `d-007` пакет длины 3, третий id не обязан быть третьим по score (`C-RK4`)
- [ ] LLM нет, повтор байт-тот же (`C-RK5`)

### S5 `mix.py`

- [ ] Сессий в пакете ≤ `MAX_PACKET`, пустых слотов нет (`C-SIZE`)
- [ ] В пакет идут целые `turns`, не саммари (`C-MX1`)
- [ ] Текст — шаблон PROMPTS §5, блок начинается с `[session_id]` (`C-MX2`)
- [ ] Self-mix нет, лишних id нет (`C-MX3`)
- [ ] `serves.jsonl` и `packet.json` держат id и текст (`C-MX4`, `C-MX5`)
- [ ] Повтор — тот же `packet_text` (`C-MX6`)

### S6 `outcome.py`

- [ ] Лаба гоняет `--source gold`, модель молчит (`C-OC1`)
- [ ] Пересечение с `useful_dialogue_ids` → good / пустое ∩ при непустом пакете → bad / пустой пакет → unclear (`C-OC2`, `C-OC3`)
- [ ] Строка outcome содержит нужные поля (`C-OC4`)

### S7 `update.py`

- [ ] Дельта только у пар из пакета × `tag_key` query (`C-DELTA`, `C-UP4`)
- [ ] good / bad / unclear двигают score как в SPEC §6 (`C-UP1`)
- [ ] `last_shown_at = closed_at` (`C-UP2`)
- [ ] Decay после инкремента, если `shows % 5 == 0` (`C-UP3`)
- [ ] Второй прогон того же `outcomes.jsonl` score не плюсует (`C-UP5`)

### `replay.py`

- [ ] Только вызывает шаги, своих промптов нет (`C-RP1`)
- [ ] На каждом диалоге порядок: S3–S5 → S6 → S7 → S2 (`C-FUTURE`)
- [ ] Первые диалоги могут получить пустой пакет (`C-RP2`)

**D1 закрыт:** все пункты выше зелёные на фикстурах. Корпус ещё не трогать.

---

## D2 — харнесс чеков

Гейт: все id из CHECKS есть в `checks.json`. Пропущенный id = провал.
HARD валит прогон.

- [ ] Харнесс гоняет блок «Что прогонять на S0» из CHECKS
- [ ] Негатив `C-NC1`: пустой пул → пустые пакеты, T совпадает с B0
- [ ] Строка чека: `{check_id, step, hard, passed, observed, expected, note}`
- [ ] SOFT пишутся, прогон жив

**D2 закрыт:** S0 HARD зелёные. `C-REPLAY` и `C-EV*` можно оставить на runner.

---

## D0 — золото пользы (Phase B, гейт: D2 зелёный)

Не трогать hold-out и корпус, пока D2 не зелёный. Разметку делает агент
`deepseek-v4-pro` по решению основателя 2026-08-28 — **NOT human gold**
(контракт и срез — [`ROUND-0-PLAN.md`](./ROUND-0-PLAN.md), DATA-AUDIT §6).

- [x] `data/d0_slice.jsonl` — 60 hold-out query (34 FAQ how-to + 6 site-troubleshoot + 20 negatives), заморожен (sha 56b5bfc0…)
- [x] `bin/label_gold_useful.py` закоммичен, зовёт `call_llm --model deepseek-v4-pro`; S2 `deepseek-v4-flash` не тронут
- [x] Промпт разметчика заморожен в PROMPTS.md §10; S2 §2–§3 байт-в-байт те же
- [x] `data/gold_useful.jsonl`: `#`-заголовок «AGENT-LABELED — NOT HUMAN GOLD», формат строк как у seed
- [x] QA: C-GD1..C-GD8 зелёные — C-GD1..5,7,8 PASS; **C-GD6 (SOFT) RESOLVED: ACCEPTED by lead 2026-08-29** (issue #51, comment 5462141906 — d-3219 seed misread, d-5711/d-4815 overridden → `[]`; checks.py re-run HARD 65/0/11, SOFT 6/0; разбор в D0-GOLD.md)
- [x] Манифест D0: `labeler_model=deepseek-v4-pro`, кавер, sha, cost отдельно от S2 `cost.json`

**D0 закрыт:** золото закоммичено одним PR (скрипт + промпт + чеки + срез + золото).

---

## D3 — аудит пола и потолка

Гейт: `audit.json` с A1–A6 есть **до** полного прогона.
Порог после увиденного hit не двигать.

- [ ] A1: доля query с хотя бы одной прошлой useful-сессией
- [ ] A2: доля золотых пар с пересечением тегов < `TAG_FIELDS_MIN`
- [ ] A3: число уникальных `tag_key`, медиана корзины
- [ ] A4: доля query с кандидатами > `MAX_PACKET`
- [ ] A5: покрытие gold (если useful-пар < 40 — L2 по пользе не публиковать)
- [ ] A6: медиана токенов транскрипта × `MAX_PACKET`
- [ ] Если порог §6.2 недостижим — правка в EVAL-PLAN **до** S2 full, с арифметикой

На S0 в `audit.json` можно написать «фикстуры, полный аудит на S1». Это не освобождает от файла.

**D3 закрыт:** числа A1–A6 лежат в run dir, пороги либо достижимы, либо явно поправлены.

---

## D4 — руки и один подсчёт классов

Гейт: T / B0 / B1 / B2 / B3 через одну функцию.

- [ ] `hit + wrong + abstain == 1.0` на том же n (`C-EV1`)
- [ ] B0: `hit == 0`, `abstain == 1` (`C-EV2`)
- [ ] Нет второй копии метрики (`C-EV3`)
- [ ] B1 фиксирует `--seed`, повтор даёт те же id (`C-EV4`)
- [ ] `per_query.jsonl` сходится с `metrics.json` (`C-EV5`)

Не путать руки:

| рука | что кладёт в пакет |
|---|---|
| T | rank.py как в SPEC |
| B0 | всегда пусто |
| B1 | случайные кандидаты S3, без рейтинга |
| B2 | топ по score, без explore |
| B3 | oracle из `useful_dialogue_ids`, если они уже в пуле |

**D4 закрыт:** таблица пяти рук считается одним кодом.

---

## D5 — один измеряемый прогон

Гейт: один run dir, пустой на входе.
Форма — RUN-PROTOCOL §3.

- [ ] `manifest.json` с sha входов, PROMPTS.md, конфига, git sha
- [ ] `checks.json` `metrics.json` `cost.json` `per_query.jsonl` `report.md`
- [ ] `raw/tag/` на каждый вызов S2
- [ ] Исходный корпус и `fixtures/` не переписаны (`C-ISO5`)
- [ ] Hold-out / золотые пары не дописывались после увиденного hit
- [ ] Промпт посреди прогона не менялся

Стадии: S0 smoke → S1 dev → S2 full → (S3) → S4 verdict.
Вердикт на S0 не ставить: мало n.

**D5 закрыт:** чужой человек может сделать `--replay` без ключа и получить те же `metrics.json`.

---

## D6 — L3

По умолчанию закрыт.

- [ ] В отчёте строка «L3 закрыт, золото есть»
- [ ] Либо отдельное решение открыть судью + калибровка
- [ ] Цифры llm-исхода не смешаны с gold-агрегатами (`C-OC5`)

---

## D7 — вердикт

Ровно одна строка EVAL-PLAN §6.4.

- [ ] Hard gates §6.1 зелёные, иначе сразу `NOT FIT`
- [ ] T.`hit` сравнён с B1.`hit` на том же n
- [ ] Названы промахи value/cost, если они есть
- [ ] Нет второй руки модели «на всякий случай»

```
FIT
FIT WITH LIMITS — <промах: цифра; рычаг>
NOT FIT — <какой gate, цифра>
```

Если `packet_tokens_p50` > 1500 при живом hit — это `FIT WITH LIMITS`, не провал идеи. Нарезку в этом проходе не делать.

---

## Каждый день, пока идёт код

- [ ] Состояние только в run dir или локальном gitignore `data/`
- [ ] Один писатель на JSONL
- [ ] Новая идея / новый промпт / новый порог = новый run id
- [ ] Отчёт за раунд — форма LAB-BRIEF §8, без абзаца

---

## Стоп. Писать заказчику, не чинить молча

- HARD не проходит без правки SPEC
- A1–A6 показывают, что порог §6.2 недостижим
- Провайдер отвечает так, что прогон нельзя повторить
- Хочется второй `bin/` или подкрутить промпт «чтобы цифра выросла»
- Хочется открыть L3 или вторую модель без отдельного решения

---

## Короткая карта «что сломалось»

| симптом | куда смотреть | чего не делать |
|---|---|---|
| Плохие теги | S2 / PROMPTS (новый run id) | крутить ранкер |
| Теги живые, поиск пустой на золотой паре | S3, `TAG_FIELDS_MIN`, A2 | крутить score |
| Поиск живой, в пакете одни и те же id | ротация, `top3_share`, B2 | поднимать `MAX_PACKET` |
| `hit` выше B1, пакет огромный | цена, вердикт WITH LIMITS | резать сессии в этом проходе |
| `hit` без B1 в отчёте | D4 не закрыт | публиковать L2 |
